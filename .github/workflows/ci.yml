name: CI Workflow
on: pull_request
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: pnpm install
      - run: pnpm lint
      - run: pnpm check-types
      - run: pnpm test
      - run: pnpm build
